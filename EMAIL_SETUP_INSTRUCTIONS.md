# تعليمات تفعيل نظام البريد الإلكتروني

## 📧 الخطوات المطلوبة على السيرفر:

### 1. تثبيت nodemailer
```bash
cd /var/www/itam-system/server
npm install nodemailer
```

### 2. إعادة تشغيل PM2
```bash
pm2 restart itam-backend
```

### 3. التحقق من عمل السيرفر
```bash
pm2 logs itam-backend --lines 20
```

يجب أن ترى:
```
✅ MongoDB Connected: localhost
📧 Email service initialized
```

---

## 🔧 الإعدادات في الواجهة:

1. سجل دخول كمسؤول (admin)
2. اذهب إلى **الإعدادات** → تبويب **إعدادات البريد**
3. فعّل الخيار **"مفعّل"**
4. أدخل البيانات التالية:

### مثال لـ Hostinger / Gmail:

**خادم SMTP (Host):**
- Hostinger: `smtp.hostinger.com`
- Gmail: `smtp.gmail.com`

**المنفذ (Port):**
- `587` (موصى به)
- أو `465` (SSL)

**اسم المستخدم (User):**
- البريد الإلكتروني الكامل
- مثال: `notifications@daralesnad.com`

**كلمة المرور (App Password):**
- **للـ Gmail:** استخدم App Password (ليس كلمة المرور العادية)
  - اذهب إلى: https://myaccount.google.com/apppasswords
  - أنشئ كلمة مرور جديدة للتطبيق
- **لـ Hostinger:** استخدم كلمة مرور البريد العادية

**بريد المرسل (From Email):**
- نفس البريد المستخدم في "اسم المستخدم"
- مثال: `notifications@daralesnad.com`

**بريد المسؤولين (Admin Notification Emails):**
- البريد الذي سيستقبل إشعارات التذاكر الجديدة
- يمكن إدخال أكثر من بريد مفصول بفاصلة
- مثال: `admin@daralesnad.com, support@daralesnad.com`

---

## ✅ اختبار النظام:

1. احفظ الإعدادات
2. اضغط على زر **"اختبار الاتصال"**
3. يجب أن تستلم بريد اختبار

---

## 📬 متى يتم إرسال البريد؟

- **تلقائياً:** عند رفع تذكرة جديدة من البوابة العامة
- يُرسل للمسؤولين المحددين في الإعدادات
- يحتوي على معلومات التذكرة كاملة

---

## 🐛 حل المشاكل:

### الخطأ: "SMTP connection failed"
- تحقق من اسم المستخدم وكلمة المرور
- تأكد من Port الصحيح (587 أو 465)
- للـ Gmail: تأكد من استخدام App Password

### الخطأ: "Authentication failed"
- Gmail: تحقق من تفعيل "Less secure app access"
- Hostinger: تحقق من كلمة المرور

### الخطأ: "Connection timeout"
- تحقق من الـ firewall على السيرفر
- تحقق من أن Port 587/465 مفتوح

---

## 📝 ملاحظات:

- البريد لن يُرسل إذا لم يتم تفعيل الإعدادات
- كلمة المرور محفوظة بشكل آمن في قاعدة البيانات
- يمكن تعديل الإعدادات في أي وقت
